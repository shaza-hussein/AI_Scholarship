# Database Verification

This script performs critical diagnostic tests on the dual-store architecture. It ensures data integrity within the DuckDB relational store and validates the semantic search and embedding retrieval capabilities within the ChromaDB vector store.

::: tests.verify_db
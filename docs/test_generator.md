# Generator Test Suite

This module executes end-to-end integration tests for the AI Generation layer. It tests multiple user intents (Discovery, Application Process, and Eligibility Constraints) to verify the dynamic routing and the LLM's adherence to the strict system prompts.

::: tests.test_generator